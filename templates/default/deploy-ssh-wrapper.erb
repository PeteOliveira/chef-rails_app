#!/usr/bin/env bash
# 
# Deploy SSH Wrapper
# App: <%= @id %> 
#
# Rendered by Chef - local changes will be replaced

echo /usr/bin/env ssh -o "StrictHostKeyChecking=no" -i "<%= @deploy_to %>/id_deploy" $1 $2 > /tmp/wrapper
/usr/bin/env ssh -o "StrictHostKeyChecking=no" -i "<%= @deploy_to %>/id_deploy" $1 $2